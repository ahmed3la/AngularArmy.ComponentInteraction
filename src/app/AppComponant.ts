import { environment } from './../environments/environment.prod';
import { Component, ViewChild, ElementRef } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './AppComponant.html'

})
export class AppComponent {

  constructor() { }
}
